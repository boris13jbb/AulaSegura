<Window x:Class="AulaSegura.App.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:AulaSegura.App.ViewModels"
        xmlns:converters="clr-namespace:AulaSegura.App.Converters"
        mc:Ignorable="d"
        Title="AulaSegura - Iniciar Sesión" 
        Height="520" 
        Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Window.Resources>
        <!-- Converters -->
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <converters:BoolToStringConverter x:Key="BoolToStringConverter"/>
        <!-- Estilos para botones -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#BDBDBD"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para TextBox -->
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
        </Style>

        <!-- Estilo para PasswordBox -->
        <Style x:Key="InputPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
        </Style>
    </Window.Resources>

    <Grid Margin="25,20,25,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Logo y Título -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,10,0,20">
            <TextBlock Text="🔒" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="AulaSegura" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#2196F3"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Control Web" 
                       FontSize="14" 
                       Foreground="#757575"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Formulario de Login -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <!-- Username -->
            <TextBlock Text="Usuario" 
                       FontWeight="SemiBold" 
                       Foreground="#424242"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="UsernameTextBox"
                     Style="{StaticResource InputTextBoxStyle}"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,5,0,12"
                     ToolTip="Ingrese su nombre de usuario"/>

            <!-- Password -->
            <TextBlock Text="Contraseña" 
                       FontWeight="SemiBold" 
                       Foreground="#424242"
                       Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox"
                         Style="{StaticResource InputPasswordBoxStyle}"
                         PasswordChanged="PasswordBox_PasswordChanged"
                         Margin="0,5,0,10"
                         ToolTip="Ingrese su contraseña"/>

            <!-- Error Message -->
            <TextBlock Text="{Binding ErrorMessage}" 
                       Foreground="#F44336"
                       FontSize="12"
                       TextWrapping="Wrap"
                       Margin="0,0,0,10"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
        </StackPanel>

        <!-- Botones -->
        <StackPanel Grid.Row="2" VerticalAlignment="Center">
            <!-- Login Button -->
            <Button Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding LoginCommand}"
                    IsDefault="True"
                    Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔑" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding IsLoading, Converter={StaticResource BoolToStringConverter}, ConverterParameter='Iniciando...|Iniciar Sesión'}"/>
                </StackPanel>
            </Button>

            <!-- Exit Button -->
            <Button Content="Salir" 
                    Command="{Binding ExitCommand}"
                    Background="Transparent"
                    Foreground="#757575"
                    BorderBrush="#BDBDBD"
                    BorderThickness="1"
                    Padding="20,8"
                    FontSize="13"
                    Cursor="Hand"
                    Margin="0,0,0,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="5"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- Footer -->
        <TextBlock Grid.Row="3" 
                   Text="© 2026 AulaSegura Control Web" 
                   FontSize="10" 
                   Foreground="#9E9E9E"
                   HorizontalAlignment="Center"
                   Margin="0,15,0,0"/>
    </Grid>
</Window>
